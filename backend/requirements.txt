fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7

# --- ML / inference -------------------------------------------------------
# TensorFlow ships different wheels per platform:
#   * Linux (Railway/CI): tensorflow-cpu is smaller (no CUDA).
#   * macOS / Windows: there is NO `tensorflow-cpu` wheel — use `tensorflow`.
# The markers below pick the right one automatically.
tensorflow-cpu; sys_platform == "linux"
tensorflow; sys_platform != "linux"

# The saved model was exported with Keras 3.13.2. Keras 3.13+ is required to
# deserialize the .keras archive (TensorFlow 2.16 alone bundles an older Keras).
keras>=3.13,<4

numpy
pandas>=2.0
scikit-learn>=1.3
h5py>=3.10
